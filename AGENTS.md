---
title: بروتوكول الوكيل — سلسبيل
status: ACTIVE
version: 2.1
authority: Security & Correctness (حوكمة إلزامية — راجع INVARIANTS.md)
last_updated: 2026-09-05
last_verified: 2026-09-05
owner: المؤسس (أبوحتاب)
source_of_truth: هذا الملف نفسه
---

# AGENTS.md — بروتوكول الوكيل

بوابة الدخول الإلزامية لأي أداة ذكاء اصطناعي (Claude Code / Cursor / غيرها) قبل أي عمل على هذا المستودع. هذا الملف يحكم **كيف** تُنفَّذ أي مهمة. لـ**ماذا** نبني، راجع `SALSABIL_CONSTITUTION.md` والإحالات في §19.

---

## 1. البروتوكول الإلزامي

لكل مهمة، بهذا الترتيب، بلا تخطي خطوة:

1. **STOP** — لا تنفيذ فوري عند استلام الطلب.
2. **READ** — هذا الملف + الوثائق ذات الصلة **بالمهمة تحديداً** (لا كل `docs/`). حدِّد الوثائق من طبيعة المهمة: جدول/عمود جديد → `DATABASE.md`؛ endpoint/service → `ARCHITECTURE.md` + `API_CONTRACTS.md`؛ قاعدة عمل → `BUSINESS_RULES.md`؛ أمان/صلاحيات → `SECURITY.md`.
3. **SEARCH** — عن قدرة/دالة/مكوّن موجود يؤدي الغرض قبل افتراض الحاجة لشيء جديد (§2).
4. **PLAN** — اعرض الخطة (النطاق، الملفات، حجم التغيير المتوقع وفق §4) على المؤسس. لا تنفّذ أثناء العرض ولا قبل الموافقة.
5. **IMPLEMENT** — فقط ما تمت الموافقة عليه صراحة.
6. **VERIFY** — اختبارات فعلية (لا افتراض نجاح) + فحوص معمارية (عزل المستأجر، الأمان، عدم التكرار).
7. **REPORT** — تقرير المهمة الإلزامي (§10)، حرفياً، في نهاية كل مهمة بلا استثناء.

هذا التسلسل يحلّ محل أي دورة عمل موثّقة سابقاً في هذا الملف.

---

## 2. Capability Before Creation

لا endpoint، لا service، لا repository، لا component جديد قبل إثبات عدم وجود قدرة موجودة تؤدي نفس المسؤولية.

- البحث يكون **بالمسؤولية لا بالاسم**: ابحث عن تكرار مفاهيمي (منطق مشابه بأسماء مختلفة)، لا تكتفِ بمطابقة حرفية للاسم.
- لا تُنشئ ملفاً جديداً لا يخدم مسؤولية غير مغطاة فعلاً — الحد الأدنى من الملفات لتحقيق المطلوب.

---

## 3. Scope Lock

لا تعديل خارج نطاق المهمة المُعلَنة، إلا لمنع خطأ أو ثغرة حقيقية — وعندها يُصرَّح به صراحة **قبل** التنفيذ لا بعده.

---

## 4. Complexity Budget

كل مهمة كبيرة تُعلَن قبل التنفيذ بحجمها المتوقع: ملفات جديدة/معدَّلة، أسطر تقريبية، تبعيات جديدة، جداول جديدة.

- دالة > 50 سطراً أو ملف > 300 سطر = **Review Trigger** يستوجب تبريراً في التقرير — ليس فشلاً تلقائياً.
- إن تجاوز التنفيذ الفعلي المتوقع بفارق كبير (مثلاً +80 متوقع مقابل +700 فعلي) — توقف واطلب المراجعة بدل الإعلان عن نجاح.

---

## 5. Never Infer Missing Architecture

إن لم تُعرَف معلومة معمارية بثقة من الكود أو الوثائق، لا تُخترَع. اذكر `UNKNOWN` صراحة + "توجد فجوة معمارية تحتاج قراراً" بدل افتراض إجابة معقولة.

عند الشك في أي قرار معماري: **توقف واسأل، لا تخمّن.**

---

## 6. Context Integrity Check

عند تعارض بين الكود والوثائق و/أو قرار مسجَّل (ADR) — لا يُحسم صمتاً باختيار مصدر واحد. يُبلَّغ كـ`CONFLICT` صراحة، بنفس آلية CONFLICT LOG في `docs/DECISIONS.md` (راجع `docs/DOCUMENTATION_RULES.md` §7).

---

## 7. لا Refactor أثناء Feature

أي إعادة هيكلة تتجاوز نطاق المهمة المطلوبة تحتاج تصريحاً مسبقاً صريحاً من المؤسس، لا قراراً منفرداً من الوكيل.

---

## 8. Fail Closed

عند عدم اليقين من صلاحية/ملكية مورد، النتيجة الافتراضية = **رفض الوصول**، لا سماحاً.

---

## 9. AI Least Privilege

لا تنفّذ عملية تتجاوز نطاق الصلاحية الموضحة في المهمة (تعديل كود ≠ صلاحية migration مدمِّر ≠ صلاحية نشر إنتاج). أي عملية destructive (`DROP`/`DELETE`/`ALTER` مدمِّر) تحتاج تصريحاً بشرياً صريحاً منفصلاً دائماً، بلا استثناء.

يدخل ضمن هذا المبدأ: خطافات Git (Husky) وdependency-cruiser المفروضة على هذا المستودع (`.husky/pre-commit`, `.husky/pre-push`, `.dependency-cruiser.cjs`) لا تُتجاوَز بـ`--no-verify` إلا بطلب صريح من المؤسس في نفس المحادثة. فشل خطاف = مشكلة حقيقية تُحل، لا تُتجاوَز.

---

## 10. شكل تقرير المهمة الإلزامي (Task Report)

يُرفَق حرفياً في نهاية كل مهمة:

```
Task: [المعرّف]
Files added / modified / removed: [عدد + أسماء]
LOC added / removed / net: [أرقام تقريبية]
New dependencies: [عدد + مبرر إن وُجد]
New DB objects (tables/columns/indexes): [عدد]
New endpoints/actions: [عدد]
Capabilities reused: [ماذا استُخدِم بدل إنشاء جديد]
New abstractions: [عدد + مبرر]
Architecture violations found: [نعم/لا + تفصيل]
Security checks: [PASS/FAIL/N/A]
Tenant isolation checks: [PASS/FAIL/N/A]
Tests: [عدد نجح/فشل]
Docs updated: [قائمة الملفات]
Outstanding risks: [صراحة، لا "لا شيء" افتراضياً بلا فحص]
```

---

## 12. No Silent Risk Acceptance — إضافة 2026-09-05

عند اكتشاف خطر خارج نطاق المهمة الحالية ولا يُصلَح ضمنها، يجب تسجيل الأربعة التالية معاً في Task
Report (لا في تعليق كود مدفون، ولا إسقاطها لمجرد كونها خارج النطاق):

1. ماذا وُجد (وصف دقيق، لا انطباع عام)
2. لماذا لم يُصلَح الآن (سبب حقيقي: خارج النطاق المُعلَن، يحتاج قراراً بشرياً، يحتاج نطاقاً أكبر، إلخ)
3. الخطر المتبقي (ما الذي قد يحدث لو بقي بلا حل، ولأي طرف)
4. `DECISION-DEBT-XXX` مسجَّل في `docs/DECISIONS.md` — إلا إذا استوفى استثناء صريح (خطر تافه لا يحتاج
   قراراً ولا إصلاحاً)، وعندها يُبرَّر عدم التسجيل صراحة في التقرير، لا بالصمت.

دفن الخطر في تعليق جانبي أو تجاهله بحجة "خارج النطاق" مخالفة مباشرة لهذا البند.

---

## 13. No Silent State Change — إضافة 2026-09-05

أي تغيير في أحد التالي يجب الإعلان عنه صراحة وبوضوح في Task Report، حتى لو كان التغيير صحيحاً ومطلوباً
ومُوافَقاً عليه ضمن المهمة:

- المعمارية (حدود نطاق، اتجاه اعتماد، طبقة جديدة)
- Schema (جدول/عمود/فهرس/قيد جديد أو مُعدَّل)
- نموذج الأمان (RLS، صلاحيات، مصادقة)
- تبعية جوهرية (dependency) جديدة أو مُرقَّاة نسخة رئيسية
- قاعدة عمل (Business Rule)
- آلة حالة (State Machine) — إضافة/حذف حالة أو انتقال
- أي تغيير آخر يؤثر في السلوك الملاحَظ أو الحدود المعمارية

الإعلان الصريح مطلوب بصرف النظر عن حجم التغيير — تغيير سطر واحد في CHECK constraint يُعلَن بنفس
وضوح إعادة هيكلة كاملة.

---

## 14. مستويات صلاحية الوكيل (Capability Levels L0–L6) — إضافة 2026-09-05

| المستوى | الوصف | أمثلة |
|---|---|---|
| L0 | قراءة فقط (Read-only) | قراءة كود/وثائق، بحث، تحليل بلا أي كتابة |
| L1 | كتابة توثيق فقط | تعديل `docs/`، `ideas/`، `specs/` — لا كود تنفيذي |
| L2 | كتابة كود غير حساس، بلا لمس Schema/أمان | مكوّن واجهة، دالة مساعدة، إصلاح لا يمس نطاقاً حساساً (§17) |
| L3 | كتابة كود يمس نطاقاً حساساً (Guardian Matrix) | تعديل منطق طلبات/مخزون/صلاحيات — يستوجب مراجعة مستقلة وفق §17 |
| L4 | تعديل Schema/RLS عبر SQL يدوي في بيئة غير إنتاجية | Migration جديدة على `dev`/`staging` فقط، لا `reefam.com` |
| L5 | نشر/تعديل بيئة إنتاجية غير مدمِّر | Deploy، تعديل متغيرات بيئة، تفعيل ميزة على إنتاج حي |
| L6 | عمليات مدمِّرة (`DROP`/`DELETE` جماعي/`force push`/حذف بيانات إنتاج) | **محظور ذاتياً على الوكيل بالكامل** |

**قاعدة صارمة:** الوكيل لا يمنح نفسه L6 تحت أي ظرف — أي عملية L6 تحتاج تصريحاً بشرياً صريحاً منفصلاً
في نفس المحادثة (امتداد مباشر لـ§9 "AI Least Privilege" القائم أصلاً، لا بديلاً عنه).

**الربط بـGuardian Matrix (§17):** كل مهمة L3 فما فوق تستوجب تحديد شدة مراجعة (`LIGHT`/`MEDIUM`/`DEEP`)
بحسب الخطر الفعلي للنطاق الممسوس، لا معاملة كل المهام بنفس الصرامة بلا تمييز — راجع §17.

---

## 15. Definition of Ready (DoR) — إضافة 2026-09-05

لا يبدأ **IMPLEMENT** (الخطوة 5 من البروتوكول في §1) قبل معرفة الحد الأدنى التالي، ولو بإجابة صريحة
`UNKNOWN`/`N/A` لكل بند لا ينطبق:

1. الهدف (ما المشكلة التي تُحل، لا الحل نفسه فقط)
2. النطاق (ما المُتضمَّن وما المُستثنى صراحة)
3. القدرات الموجودة القابلة لإعادة الاستخدام (نتيجة SEARCH، §2)
4. التبعيات (على أي نطاقات/جداول/خدمات أخرى)
5. الحدود الأمنية (من يُخوَّل فعل ماذا بعد هذا التغيير)
6. تغييرات البيانات المحتملة (Schema جديد/معدَّل، إن وُجد)
7. انتقالات الحالة المحتملة (State Machine، إن انطبق)
8. استراتيجية الاختبار (كيف سيُثبَت أن هذا يعمل، لا افتراض النجاح)

إذا لم تكتمل هذه الثمانية: **الحالة = NOT READY** — يُبلَّغ صراحة، لا يُنفَّذ جزئياً على أمل الإكمال لاحقاً.

---

## 16. Definition of Done (DoD) — إضافة 2026-09-05

المهمة **مكتملة** فقط بعد تحقُّق ما ينطبق منها فعلياً (لا افتراض الانطباق ولا إسقاطه بلا تبرير):

- الكود مكتوب ومطابق للنطاق المُعلَن
- الاختبارات (وحدة/تكامل حسب الحاجة) مكتوبة ومُنفَّذة فعلياً — لا افتراض نجاح
- `npm run arch:check` نظيف
- فحص أمني حسب حساسية التغيير (راجع §17)
- فحص عزل المستأجرين (Tenant Isolation) إن مسّت المهمة بيانات تاجر
- فحص أداء إن انطبق (تغيير على مسار حرج/عالي التردد)
- تحديث `INVARIANTS.md` إن مسَّت المهمة Invariant قائمة (تغيير حالتها، إضافة دليل جديد، إلخ)
- تحديث الوثائق ذات الصلة (§2 من البروتوكول أعلاه)
- Guardian Review إن كان إلزامياً وفق §17
- `git status` نظيف (لا ملفات غير مُفسَّرة، لا تغييرات خارج النطاق المُعلَن)
- Task Report كامل (§10) مُرفَق حرفياً

مهمة يُعلَن اكتمالها بلا تحقُّق فعلي من البند المنطبق = ادعاء غير موثوق، يُعامَل كخطأ بروتوكول لا كتفصيل
شكلي.

---

## 17. Guardian Matrix — المراجعة المستقلة الإلزامية — إضافة 2026-09-05

مراجعة مستقلة (Guardian Review — مراجع/جلسة منفصلة عن من نفَّذ التغيير) **إلزامية** لأي مهمة تمس أياً
من النطاقات التالية. "إلزامي" هنا يعني إلزامية **وجود** مراجعة مستقلة، لا أن كل هذه المراجعات بنفس
العمق — الشدة (`LIGHT`/`MEDIUM`/`DEEP`) تُقرَّر عند التصنيف بحسب الخطر الفعلي للتغيير بعينه (حجمه، مدى
تأثيره، هل يمس بيانات حية)، لا بحسب اسم الفئة وحده:

| النطاق | شدة مقترحة افتراضية | ملاحظة |
|---|---|---|
| Authentication | DEEP | لا مصادقة حقيقية موجودة بعد اليوم (`ADR-012`) — أي عمل عليها بالذات شديد الحساسية بطبيعته |
| Authorization / RBAC | DEEP | يحدد من يفعل ماذا عبر كل النظام |
| RLS | DEEP | آخر خط دفاع على مستوى قاعدة البيانات؛ خطأ هنا صامت حتى الاختبار الحي |
| Tenant Isolation | DEEP | مطبَّقة اليوم في كود التطبيق لا RLS (`orders.service.ts`) — تعديلها يحتاج دليلاً حياً لا فحص كود فقط |
| Financial logic | DEEP | حساب سعر/عمولة/إجمالي — لا هامش خطأ مقبول |
| Inventory Concurrency | DEEP | سباقات تزامن (TOCTOU) لا تظهر إلا تحت اختبار حي متزامن فعلي (راجع `ADR-022`) |
| Orders State Machine | MEDIUM–DEEP | تغيير قيد الانتقالات/الفاعلين نفسه = DEEP؛ إضافة حقل عرض غير سلوكي = MEDIUM |
| DB Schema | MEDIUM–DEEP | جدول بيانات حساسة (مالية/هوية) = DEEP، جدول عرض/محتوى عام = MEDIUM |
| Public API | MEDIUM | لا API خارجية مكشوفة بعد (`docs/SECURITY.md §11`) — يُعاد تقييمها فعلياً عند بناء أول واحدة |
| Security Boundary | DEEP | أي حد بين "مسموح"/"ممنوع" (عزل، صلاحية، تحقق مدخل) |
| Domain Extraction | MEDIUM | فصل نطاق لخدمة مستقلة — تعقيد معماري، لا خطر أمني مباشر بالضرورة |
| Event Contracts | MEDIUM | لا سجل أحداث مركزي مبني بعد (`PROPOSED`) — تُعاد المراجعة فعلياً عند بنائه |
| AI Agent Authority | DEEP | صلاحيات الوكيل نفسه (§14)، أي توسيع لها |
| Automation Authority | DEEP | أي أتمتة تنفّذ عملية بلا تأكيد بشري مباشر |
| Production Migration | DEEP | لا نظام Migrations رسمي بعد (`docs/DATABASE.md §8`) — كل SQL على قاعدة حية، خطر لا رجعة فيه |

**قاعدة الشدة:** LIGHT لتغيير سطحي منخفض الخطر داخل نطاق حساس (تسمية، تعليق، إعادة ترتيب بلا تغيير
سلوك)؛ MEDIUM لتغيير سلوكي محدود الأثر؛ DEEP لتغيير يمس اتساقاً مالياً/أمنياً/بيانات حية مباشرة.
المصنِّف (الوكيل أو المؤسس) يُبرِّر اختياره في Task Report، لا يفترضه صامتاً.

---

## 18. Golden Paths — المفهوم فقط — إضافة 2026-09-05

Golden Path = رحلة مستخدم كاملة (من الألف للياء) يعتمد عليها النظام فعلياً، ومُثبَتة باختبار E2E/تكامل
حقيقي — لا وصفاً نظرياً لتدفق مقترح.

**هذا الملف لا يُملي قائمة Golden Paths.** القائمة الفعلية — مستخرجة من الاختبارات الموجودة فعلياً في
المستودع، لا مفترضة — تُسجَّل وتُحدَّث في `INVARIANTS.md` (قسم Golden Paths). أي مهمة تمس نطاقاً يظهر
في Golden Path مُسجَّل تستوجب إعادة تشغيل/فحص ذلك المسار ضمن DoD (§16) قبل إعلان الاكتمال.

---

## 19. إحالات (لا تكرار محتوى)

- المعمارية والقواعد غير القابلة للكسر (لا استدعاء DB مباشر من الواجهة، لا حساب سعر في الواجهة، عزل tenant عبر JWT فقط) → `SALSABIL_CONSTITUTION.md` §4، `docs/ARCHITECTURE.md`.
- الأمن → `docs/SECURITY.md`.
- قاعدة البيانات → `docs/DATABASE.md`.
- قواعد التوثيق نفسها (التسلسل الهرمي، الحالات، مستوى الدليل) → `docs/DOCUMENTATION_RULES.md`.
- القرارات المعمارية وسجل التعارضات → `docs/DECISIONS.md`.
- قواعد الذكاء الاصطناعي **كمنتج داخل سلسبيل** (حكيم) — مختلفة عن هذا الملف الذي يحكم أداة الذكاء الاصطناعي المستخدَمة في **بناء** سلسبيل → `docs/AI_RULES.md`.
- سجل الثوابت الملزمة (Invariants)، حالتها الفعلية، وGolden Paths المُستخرَجة من الاختبارات الحقيقية → `INVARIANTS.md` (راجع §14، §17، §18 أعلاه).

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
