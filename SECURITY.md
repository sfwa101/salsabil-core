# قواعد الأمان — غير قابلة للتفاوض

راجع الكامل: SALSABIL_CONSTITUTION.md §26

1. RLS مفعّل على كل جدول متعدد المستأجرين.
2. لا سعر من العميل يُصدَّق أبداً — يُعاد حسابه من الخادم دائماً.
3. كل تعديل (mutation) يتطلب مصادقة (Authentication) والتحقق من الصلاحية (Authorization).
4. لا أسرار (API keys) في الكود أو الواجهة — فقط في متغيرات بيئة (.env، غير مرفوع لـ Git).
5. كل عملية حساسة تُسجَّل في audit_log.