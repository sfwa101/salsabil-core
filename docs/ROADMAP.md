---
title: خارطة الطريق
status: ACTIVE
version: 1.31
last_updated: 2026-09-08
owner: المؤسس (أبوحتاب)
source_of_truth: هذا الملف (التتبع الحي)، SALSABIL_CONSTITUTION.md §23 (الخطة الأصلية + تصحيح 14→18 يوماً)
---

# خارطة الطريق

---

## Vision — Evidence: `CONSTITUTION` §1

نظام تشغيل حضاري عربي-إسلامي، يبدأ بريف المدينة وينمو تدريجياً ليشمل كل مناحي الحياة تحت مظلة ديوان.

---

## Current Phase — القطعة الرأسية الأولى (Vertical Slice)

**الحالة الرسمية للمشروع:** `Phase 1 Complete — Live in Production` (قرار مؤسس مباشر، 2026-09-03، `FULL-DOCS-AUDIT-AND-SYNC-UP-TO-DAY18`).
**المحطة القادمة:** `Phase 2 Preparation (Contextual Worlds & Ecosystem Scaling)`.

> **⚠️ ملاحظة دقة صريحة (لا تُقرأ الحالة أعلاه بلا هذا السطر):** الدليل الحي الفعلي وراء "Live in Production" هو `staging.reefam.com` تحديداً — تحقَّقتُ حياً أنه يعمل بنفس الكود المدفوع (`docs/CHANGELOG.md` اليوم 18)، بما فيه إصلاح `/cart` الحرج. **لا قرار أو فعل موثَّق في هذا المستودع بتحويل نطاق إنتاج منفصل بلا بادئة "staging"**، ولا تأكيد أن المؤسس أتمّ فعلياً "أول شراء حقيقي" بنفسه بعد (الإرشادات سُلِّمت له، التنفيذ اليدوي متروك له — راجع اليوم 18). القرار أعلاه بصياغته الحرفية نُفِّذ كما طلب المؤسس صراحة؛ هذا السطر توثيق للفجوة بين "الكود/البيئة جاهزان ومُتحقَّق منهما" و"عميل حقيقي أول اشترى فعلاً"، لا اعتراض عليه.

**الهدف:** ريف المدينة — حي السوبرماركت، بنطاق كامل (عميل + تاجر + إدارة) يعمل من الألف للياء.

| اليوم | المهمة | الحالة |
|---|---|---|
| 0 | إعداد البيئة (Git, Node, VS Code, هيكل مجلدات) | `DONE` |
| 1 | Khalil Engine (types, service) + 6 ملفات ذاكرة | `DONE` |
| 2 | ربط Supabase الفعلي + جدول users (RLS) | `DONE` |
| 3 | محرك المنتج (Catalog) — categories, products, inventory | `DONE` |
| 4 | بوابة التاجر (merchants, stores, tenant isolation) | `DONE` — commit `2de9345`، راجع `docs/DECISIONS.md → ADR-006` |
| 5 | واجهة العميل (Storefront) — Next.js 16، Reef City Storefront | `DONE` — commit `2de9345`، راجع `docs/DECISIONS.md → ADR-006` |
| 6 | معمارية الثيمات متعددة العوالم (Multi-World Theming) | `DONE` — راجع `docs/DECISIONS.md → ADR-007` (الآلية IMPLEMENTED، حالة ADR-007 نفسها لا تزال PROPOSED بانتظار اعتماد المؤسس) |
| 7 | السلة (Cart) | `DONE` — راجع `docs/DECISIONS.md → ADR-008`، `docs/DATABASE.md §3` |
| 8 | Checkout (سلة → طلب PENDING) | `DONE` — `CHECKOUT-001`، راجع `docs/DECISIONS.md → ADR-009` |
| 9 | دورة حياة الطلب الكاملة (Orders) | `DONE` — `ORDERS-002`، راجع `docs/DECISIONS.md → ADR-010`، `specs/orders/README.md` |
| 10 | طلبات التاجر | `DONE` — بوابة تاجر فعلية (دخول بالهاتف + جلسة حقيقية + قائمة طلبات + تغيير حالة)، راجع `docs/DECISIONS.md → ADR-012`، `specs/merchant/SPEC.md` |
| 11 | لوحة الإدارة الأساسية | `DONE` — تسجيل دخول `platform_admin`، إدارة تجار، رؤية/تحكم كل الطلبات، سجل تدقيق من `order_status_history`. راجع `docs/DECISIONS.md → ADR-013`، `specs/admin/SPEC.md` |
| 12 | يوم الأمان الكامل | `DONE` — commit `dd13cea`، `audit_log` عام، إصلاح IDOR في السلة، تحقق مدخلات (`zod`)، Rate Limiting على الدخول. راجع `docs/DECISIONS.md → ADR-014`، `ADR-015` |
| 13 | الاختبار الشامل (E2E-DAY13-001) | `DONE` — اختبار تكامل شامل لرحلة "ريف المدينة" الكاملة (زائر → سلة → Checkout → تاجر أ → عزل مستأجرين ضد تاجر ب حقيقي (تطبيقياً وRLS مباشرة) → تاجر أ يكمل → إدارة تُسلِّم → `order_status_history`/`audit_log`)، 8 سيناريوهات، ذاتي التنظيف. المجموع الآن 97 اختباراً، صفر حالات تسابق (Race Conditions) متبقية معروفة في هذا الملف. commits `c3b28eb`، `5b3cd2a`، `68efdf4` |
| 14 | مرحلة الواجهة الأمامية (UI/Frontend) — إصلاح خطأ 500 الحرج في `/cart`/`/checkout` (`src/proxy.ts`) | `DONE` — اكتُشف الخطأ حياً عبر تدقيق `UI-FRONTEND-AUDIT-001` (زيارة مباشرة بلا كوكي سابقة تُسقِط الصفحتين)، أُصلِح عبر `proxy.ts` (لا `middleware.ts` — اصطلاح Next.js 16 الجديد، تحقُّق حي من `node_modules/next/dist/docs`). راجع `docs/DECISIONS.md → ADR-016` |
| 15 | مرحلة الواجهة الأمامية (UI/Frontend) — Header/Navigation موحّد + عدّاد سلة حي | `DONE` — مكوّن `Header.tsx` عبر كل صفحات `(reef)`؛ فصل صريح بين قراءة/إنشاء هوية السلة لمنع سباق حقيقي اكتُشف أثناء التخطيط (`getItemCountForSession` لا تُنشئ سلة). راجع `ADR-016` |
| 16 | مرحلة الواجهة الأمامية (UI/Frontend) — صفحة تتبّع الطلب `/order/[id]` | `DONE` — رابط UUID دائم كآلية تفويض لعميل ضيف بلا حساب (`ordersService.getOrderForCustomerView`)، لا يعرض عنوان/هاتف العميل. `CheckoutForm` يُوجِّه إليها بعد نجاح الطلب بدل حالة داخلية مؤقتة. المجموع الآن 104 اختباراً (وحدة + تكامل)، كل الثلاثة بنود مُتحقَّق منها حياً عبر Playwright (200/404، console نظيف، رابط يعمل من متصفح منفصل كلياً بلا جلسة). راجع `ADR-016`، `docs/SECURITY.md §16` |
| 17 | التجهيز للإنتاج (Production Deployment) | `DONE` — فحص متغيرات بيئة حي كامل + `.env.example`، `npm run build` نظيف صفر تحذيرات، `scripts/schema-setup.sql`/`scripts/seed-test-accounts.sql` (أول خطوة فعلية نحو Migrations رسمية). راجع `docs/DECISIONS.md → ADR-017` |
| 18 | الإطلاق الحي (Live Launch) | `DONE` — تحقُّق حي أن `staging.reefam.com` يعمل بنفس الكود (إصلاح `/cart` يعمل هناك فعلياً)، `scripts/test-first-real-purchase.e2e.ts` (سكربت رحلة شراء كامل، مُتحقَّق منه ضد dev لا staging — راجع ملاحظة الدقة أعلاه)، إرشادات يدوية مُسلَّمة للمؤسس. المجموع يبقى 104. راجع `ADR-017` |
| 19 | Context Engine — Migration `worlds`/`user_personas` على Supabase (dev) | `DONE` — DDL يدوي عبر SQL Editor (`scripts/day19-context-engine-schema.sql`)، RLS بالنمط 2 (قفل كامل)، seed صف `individuals`، backfill لـ5 مستخدمين `customer` حاليين، تحقُّق حي 9/9 بمحاولات إدراج فاشلة متعمَّدة (`scripts/day19-context-engine-seed-and-verify.ts`). لا مستهلك كود بعد (`types.ts`/`khalil.repository.ts` مؤجَّلان لليوم 20 صراحة). راجع `ADR-018`، `CONFLICT-006`، `docs/DIWAN_VISION.md` |
| 20 | Context Engine — `types.ts` (`World`/`UserPersona`) + `khalil.repository.ts` (4 دوال جديدة) | `DONE` — إضافي بحت، بلا لمس أي دالة/نوع قائم. 10 اختبارات وحدة جديدة (`khalil.repository.test.ts`، تموّه `supabaseAdmin` مباشرة — أول ملف بهذا النمط في المستودع). المجموع الآن 114. لا تعديل على `service.ts` — مؤجَّل لليوم 21 صراحة |
| 21 | Context Engine — ربط `service.ts` (`ensureIndividualPersona` + `findOrCreateCustomerByPhone`) | `DONE` — Checkout الحقيقي ينشئ user+persona معاً الآن، مُتحقَّق منه حياً (اختبار تكامل جديد + idempotency). **اكتشاف حي مهم أثناء التنفيذ:** تسرّب بيانات حقيقي في 4 ملفات اختبار قائمة (ترتيب حذف `users`/`user_personas` الخاطئ يفشل بصمت بقيد FK) — اكتُشف، نُظِّف (8 صفوف)، وأُصلِح في كل الملفات الأربعة. المجموع الآن 120. راجع `ADR-019` للتفصيل الكامل |
| 22 | Context Engine — سياسة حذف صريحة (`ON DELETE RESTRICT`) + إغلاق نهائي | `DONE` — معالجة جذر اكتشاف اليوم 21 (لا فقط عرَضه): `user_personas.user_id` أصبح `ON DELETE RESTRICT` صريحاً (`scripts/day22-user-personas-fk-policy.sql`، DDL يدوي)، مرتبط صراحة بـ`OPEN_QUESTION` Soft/Hard Delete في `docs/DATABASE.md §7` (يحسم جزءاً ضيقاً فقط). تحقُّق حي: حذف مستخدم اختباري له شخصية رُفض فعلياً بكود `23503`. راجع `ADR-020` |

> **▶️ بدء BAYAN-HOME-FEED-001 (الأيام 23-32 مخطَّطة، 2026-09-05):** خلاصة بيان الرئيسية الجديدة لريف
> المدينة — أول تنفيذ فعلي لمحرك بيان (`CONCEPTUAL` منذ الدستور v1.0)، مبدّل عوالم حي يستهلك جدول
> `worlds` (اليوم 19)، تصميم استجابي كامل (موبايل/تابلت/ديسكتوب)، توسيع نظام الثيمات. خطة Spec+Plan
> كاملة اعتُمدت صراحة عبر EnterPlanMode/ExitPlanMode قبل أي كود. راجع `docs/DECISIONS.md → ADR-021`
> فصاعداً لكل يوم من هذه السلسلة.
>
> **✅ إغلاق BAYAN-HOME-FEED-001 بالكامل (اليوم 32، 2026-09-06):** الأيام 23-32 كلها `DONE` — راجع
> صف اليوم 32 أدناه للتفصيل الكامل (E2E شامل + مراجعة Outstanding Risks + توثيق ختامي). **النطاق
> المتبقي المُعلَن صراحة (لا نسيان):** الهوية البصرية `slug: bayan` (`docs/UI_UX_SYSTEM.md §8.4`)
> غير مُستخدَمة في صفحة الخلاصة نفسها (تبقى `data-world="reef"`)؛ `OPEN_QUESTION-001`/`002`
> (`docs/DOMAIN_MAP.md`) غير محسومين؛ مقاسات الشاشة المتعددة مُختبَرة لخلاصة بيان (`/`) تحديداً لا
> كل صفحات `(reef)`؛ ~~لا مكوّن UI لاختيار الثيم الشخصي بعد~~ **أُغلِق، راجع
> `BAYAN-CLOSEOUT-UI-GAPS` أدناه.** راجع `docs/DOMAIN_MAP.md → بيان` للتفصيل الكامل.
>
> **✅ BAYAN-CLOSEOUT-UI-GAPS (2026-09-06):** إغلاق صريح لبندَي `RE-EVALUATE` الوحيدين من مراجعة
> اليوم 32 — `BottomNav.tsx` حقيقي (4 وجهات: الرئيسية/الأقسام/طلباتي/حسابي) + `PersonalThemeSheet.tsx`
> (نقطة الدخول الوحيدة لاختيار الثيم الشخصي، تستهلك `persistPersonalTheme`/`persistPersonalMode`
> من اليوم 30). تحقُّق حي كامل 12/12 (`scripts/bayan-closeout-ui-gaps-verify.ts`). **هذا يُغلِق
> الفجوتين الوحيدتين المتبقيتين من كامل دفعة بيان** — النطاق المتبقي الحقيقي الآن يقتصر على: الهوية
> البصرية `slug: bayan`، `OPEN_QUESTION-001`/`002`، ومقاسات شاشة بقية `(reef)` (بند `ATTB` أصلاً).

| 23 | بيان (Bayan) — الباك-إند: `posts`/`post_media`/`post_products` + `bayan.repository.ts`/`bayan.service.ts` (BAYAN-HOME-FEED-001) | `DONE` — DDL يدوي عبر SQL Editor، RLS بالنمط 1 (قراءة عامة للمنشور فقط)، تحقُّق حي 8/8 (قفل RLS للمسودة، محاولات إدراج فاشلة FK×2+CHECK، حذف متسلسل). 20 اختباراً وحدة جديدة، المجموع 103 وحدة/140 إجمالاً. لا واجهة بعد — راجع `ADR-021` |
| 24 | بيان (Bayan) — لوحة إدارة المنشورات: `src/app/admin/posts/` (قائمة/إنشاء/تعديل) + `PostForm.tsx` (BAYAN-HOME-FEED-001) | `DONE` — أول نموذج إنشاء متعدد الحقول في التطبيق (صفوف صور ديناميكية بمنتقي رابط none/product/recipe + بانِ وصفة متداخل، + إدارة الرف الأفقي `post_products` مستقلة تماماً — أُضيفت لاحقاً بنفس اليوم رداً على سؤال مؤسس صريح لسد فجوة كانت ستظهر عند اليوم 27)، `actions.ts`+Zod+row-component (`AdminPostRow.tsx`) بنفس نمط `OrderRow.tsx`/`AdminMerchantRow.tsx`. 11 اختباراً وحدة جديدة، المجموع 114 وحدة/151 إجمالاً. تحقُّق حي كامل عبر متصفح حقيقي + Supabase حقيقي (`scripts/day24-bayan-admin-posts-verify.ts`، 14/14). راجع `docs/CHANGELOG.md` |
| 25 | بيان (Bayan) — مكوّنات مشتركة: `HorizontalShelf.tsx`، `BottomSheet.tsx` (BAYAN-HOME-FEED-001) | `DONE` — بناء + تحقُّق حي معزول فقط (تأكيد مؤسس صريح — لا ربط ببيانات حقيقية بعد، مؤجَّل لليومين 26-27). مكوّنان عامّان: `HorizontalShelf` (رف تمرير أفقي بعنوان/رسالة فراغ اختياريين، يخدم رف `post_products` ورفوف "اشتريت مؤخراً"/"الأكثر مبيعاً" `CONSTITUTION §7.1` بنفس الـAPI)، `BottomSheet` (نافذة سفلية منزلقة عامة بلا محتوى محدد سلفاً، تُغلَق بالزر/النقر على الخلفية/`Escape`). تحقُّق حي عبر صفحة معاينة مؤقتة + سكربت Playwright مؤقت، كلاهما حُذف بعد التأكد (10/10) — لا أثر دائم بخلاف المكوّنين أنفسهما. راجع `docs/CHANGELOG.md` |
| 26 | بيان (Bayan) — إعادة هيكلة ترويسة العميل: `FeedTopBar.tsx`، `StoryBar.tsx`، `FeedTabBar.tsx` (BAYAN-HOME-FEED-001) | `DONE` — طبقة بصرية غالباً ثابتة أعلى المحتوى القائم في `src/app/(reef)/page.tsx` بلا لمسه (الخلاصة الفعلية تستبدله لاحقاً، اليوم 27). مبدّل عوالم (بصري فقط، منطق فعلي مؤجَّل لليوم 29)، عنوان بعناوين وهمية عبر `BottomSheet`، أيقونتا باركود/بحث بتنبيه "قريباً" مؤقت، `StoryBar` (`HorizontalShelf`) فوق الأحياء الحقيقية، `FeedTabBar` كحالة عميل عبر رابط الصفحة (`?tab=`) تُغذّي استعلام الخلاصة لاحقاً. `lucide-react` مؤكَّد كمكتبة الأيقونات الفعلية (`docs/UI_UX_SYSTEM.md §6`). تحقُّق حي كامل على Viewport موبايل حقيقي (390×844، `scripts/day26-feed-header-verify.ts`، 14/14: RTL، توكنز لونية حية لا Hex، تمرير أفقي فعلي، `sticky` تبويبات، حالة الرابط). راجع `docs/CHANGELOG.md` |
| 27 | بيان (Bayan) — الخلاصة الفعلية: `Feed.tsx`، `PostCard.tsx`، `ReelsShelfPlaceholder.tsx` + تمرير لانهائي (BAYAN-HOME-FEED-001) | `DONE` — `bayan.service.listFeed()` لم يُعدَّل (مكتمل أصلاً منذ اليوم 23)؛ العمل الفعلي في طبقة العرض + قدرة كتالوج جديدة صغيرة (`catalogService.getProductsByIds`، أول اختبار وحدة لكتالوج). `PostCard` (carousel صور+snap، مؤشرات نقاط، caption، رف منتجات مرتبط)، `Feed` (أول `IntersectionObserver` في المستودع للتمرير اللانهائي)، رفوف ريلز نائبة متداخلة كل 4 منشورات. `<CategoryCard>`/قائمة الأقسام حُذفتا من `page.tsx` نهائياً كما أُعلِن صراحة في اليوم 26 (تصفّح الأحياء عبر `StoryBar`). تحقُّق حي كامل (`scripts/day27-feed-rendering-verify.ts`، 11/11): ترتيب `priority` تنازلياً، استبعاد المسودات بنيوياً قبل/بعد التمرير، تحميل الصفحة الثانية فعلياً عبر التمرير. Guardian غير مطلوب (خارج Guardian Matrix). المجموع الآن 126 اختباراً. راجع `docs/CHANGELOG.md` |
| 28 | بيان (Bayan) — Product/Recipe Bottom Sheet: `ProductSheetContent.tsx`، `RecipeSheetContent.tsx`، `ScrollHideBar.tsx` (BAYAN-HOME-FEED-001) | `DONE` — يُغلِق Outstanding Risk #1 من تقرير اليوم 27 (النقر على صورة carousel مرتبطة يفتح الآن Bottom Sheet فعلياً). وضع "منتج" يُغلِّف `ProductOptions.tsx` القائم بلا تعديل؛ وضع "وصفة" عدّاد عائلة يُعيد الحساب عبر `scaleRecipeIngredientsAction` (تمريرة لـ`bayanService.scaleRecipeQuantities` القائمة)، إضافة فردية أو "أضف الكل" (صُحِّح أثناء التحقُّق ليعكس نجاحاً حقيقياً لا وهمياً عند فشل جزئي). `ScrollHideBar` يُخفي/يُظهر FeedTopBar+StoryBar+FeedTabBar معاً بالتمرير — **فجوة معمارية "BottomNav" غير موجود اكتُشفت وأُبلِغت للمؤسس صراحة (AskUserQuestion)، القرار: تجاهلها الآن**؛ Header.tsx استُثني أيضاً (قرار نطاق موثَّق). تحقُّق حي كامل (`scripts/day28-product-recipe-sheet-verify.ts`، 13/13، هاتف حقيقي): كلا الوضعين يضيفان للسلة فعلياً (فحص مباشر عبر service_role على cart_items، لا افتراض من رسالة الواجهة)، سلوك التمرير مُتحقَّق منه (اكتشاف تقني: Tailwind v4 يستخدم خاصية `translate` منفصلة لا `transform`). Guardian غير مطلوب. المجموع يبقى 126 (ميزة UI/تكامل، تحقُّق حي لا وحدوي). راجع `docs/CHANGELOG.md` |
| 29 | بيان (Bayan) — مبدّل العوالم الحقيقي: `WorldSwitcher.tsx` (BAYAN-HOME-FEED-001) | `DONE` — يستبدل زر Globe الوهمي (اليوم 26) بمبدّل حقيقي يقرأ `khalilService.listActiveWorlds()` (الأيام 20/23) عبر `listActiveWorldsAction` جديدة؛ عدد الدوائر ديناميكي بلا Hardcode، نمط Stories بأنيميشن دخول/خروج حقيقي (opacity+scale+translate متدرّج، دخول متتابع). `FUNCTIONAL_WORLD_SLUGS` يحدد العوالم الجاهزة وظيفياً (`individuals` فقط اليوم) بمعزل عن نشاطها في القاعدة — أي صف مستقبلي يظهر معطَّلاً+"قريباً" تلقائياً بلا تعديل كود. **إصلاح تراجع حقيقي مكتشَف أثناء البناء:** `BottomSheet.tsx` (اليوم 25) كان قد انحصر بصرياً أعلى الشاشة فقط منذ `ScrollHideBar` (اليوم 28) بسبب "containing block" لـ`position:fixed` تحت أي سلف بخاصية `transform` — أُصلِح عبر `createPortal` لكلا المكوّنين (+`@types/react-dom` devDependency). تحقُّق حي كامل (`scripts/day29-world-switcher-verify.ts`، 8/8، هاتف حقيقي، يتضمن فحص تغطية الشاشة الكاملة يكتشف فئة هذا الخلل تحديداً) + إعادة تشغيل تحقُّقَي اليومين 27/28 كفحص ارتداد (11/11، 13/13، بلا تأثر). Guardian غير مطلوب. المجموع يبقى 126. راجع `docs/CHANGELOG.md` |
| 30 | بيان (Bayan) — محور التفضيل الشخصي للثيمات: `personal-theme-registry.ts` (BAYAN-HOME-FEED-001) | `DONE` — محور مستقل تماماً عن `WorldSlug`/`[data-world]` (`theme-registry.ts`، `ADR-007`): 4 ثيمات (`masculine`/`feminine`/`youth` Pastel + `simplified` تباين أعلى وخط أكبر) × `light`/`dark` = 8 تركيبات، نطاق CSS Variables منفصل كلياً (`--sb-pt-` على `html[data-personal-theme][data-personal-mode]`، لا `--sb-` الخاصة بـ`[data-world]`) لمنع أي تصادم وراثة. تخزين `localStorage` فقط (`src/lib/personal-theme.ts`، قرار مسبق اليوم 23) يُطبَّق عبر `PersonalThemeInitializer.tsx` في `layout.tsx`. **بنية تحتية فقط عمداً — لا مكوّن UI لاختيار الثيم بعد** (لا نقطة دخول مذكورة في الموجّه)، بنفس نمط اليوم 6 (آلية قبل مستهلك واجهة). تحقُّق حي كامل عبر صفحة معاينة + سكربت Playwright مؤقتين (26/26: تطابق كل توكن حرفياً بالسجل لكل تركيبة، نجاة الاختيار من إعادة التحميل، حجم الخط الجذري يتضاعف فعلياً لـ`simplified` فقط، صفر أخطاء Console) على viewport هاتف حقيقي (390×844) — **كلاهما حُذف بعد التأكد** (نفس نمط اليوم 25، لا صفحة دائمة تستهلك المحور بعد). Grep تدقيقي شامل أكّد صفر Hex خارج `theme-registry.ts`/`personal-theme-registry.ts`/`globals.css`. Guardian غير مطلوب (UI/ثيمات، خارج Guardian Matrix). المجموع يبقى 126 (بنية تحتية بلا منطق خدمة قابل للاختبار الوحدوي بمعزل عن DOM — تحقُّق حي بديل، لا `jsdom` في المشروع). راجع `docs/CHANGELOG.md`، `docs/UI_UX_SYSTEM.md §8.6` |
| 31 | بيان (Bayan) — تخطيط Responsive لخلاصة بيان: `Feed.tsx`/`BottomSheet.tsx`/`HeaderSearchBar.tsx` (BAYAN-HOME-FEED-001) | `DONE` — مقياس عرض محتوى موحَّد (`max-w-2xl`→`md:max-w-4xl`→`xl:max-w-6xl`) عبر `Header`/`FeedTopBar`/`FeedTabBar`/`main`. `Feed.tsx`: Grid بدل عمود Flex واحد (1 عمود موبايل → 2 `md` → 3 `xl`)، `ReelsShelfPlaceholder`/حارس التمرير اللانهائي بـ`col-span-full`. `BottomSheet.tsx`: نافذة سفلية بعرض كامل على الموبايل → Modal مُمركَز (`max-w-lg`) من `md` فصاعداً — إعادة استخدام نمط `WorldSwitcher.tsx` القائم فعلياً منذ اليوم 29 حرفياً، لا تصميم جديد. رفوف `PostCard`/`StoryBar`/`ReelsShelfPlaceholder` تكبر تدريجياً بنفس المقياس ("رفوف منتجات أعرض"). شريط بحث جديد `HeaderSearchBar.tsx` (Client صغير، توست "قريباً" نفس نمط قائم) مدموج في `Header.tsx` من `lg` فصاعداً؛ زر بحث `FeedTopBar.tsx` يُخفى بـ`lg:hidden` تفادياً للازدواج. تحقُّق حي كامل دائم (`scripts/day31-responsive-verify.ts`، **25/25**، ثلاثة أحجام حقيقية 375×667/768×1024/1440×900 ضد صفحة `/` الحقيقية بزرع بيانات ذاتية التنظيف): لا انسكاب أفقي، عدد أعمدة الشبكة يطابق المتوقَّع (1/2/3) بدقة، ظهور/اختفاء `HeaderSearchBar` مقابل زر بحث `FeedTopBar` حصرياً بحسب الحجم، `BottomSheet` سفلي/Modal بحسب الحجم بدقة، `WorldSwitcher` يعمل بلا خطأ على الأحجام الثلاثة، صفر أخطاء Console. **اكتشاف أثناء التحقُّق (خلل في سكربت الاختبار لا في الواجهة):** نقر خلفية `WorldSwitcher` بعرض الشاشة الكاملة يُصادف دائماً الصندوق المُمركَز المرسوم فوق مركزها بالضبط (فخّ Playwright بحت) — صُحِّح باستخدام `Escape` بدل نقر الخلفية (الآلية موجودة فعلياً في كلا المكوّنين). Guardian غير مطلوب. المجموع يبقى 126 (تعديلات عرض/CSS بحتة، لا منطق خدمة جديد). راجع `docs/CHANGELOG.md`، `docs/UI_UX_SYSTEM.md §7` |
| 32 | بيان (Bayan) — إغلاق نهائي شامل: E2E كامل + مراجعة Outstanding Risks + توثيق ختامي (BAYAN-HOME-FEED-001) | `DONE` — **آخر يوم في الدفعة.** `scripts/day32-bayan-full-e2e.ts` (جديد، دائم) — رحلة واحدة مستمرة (نمط `test-first-real-purchase.e2e.ts`) مُشغَّلة مرتين كاملتين (هاتف 390×844 + سطح مكتب 1440×900، بذرة بيانات مستقلة ذاتية التنظيف لكل تكرار): خلاصة حقيقية → تمرير لانهائي (10→11) → منشور منتج (BottomSheet) → إضافة سلة حقيقية (فحص `cart_items` مباشر) → منشور وصفة → تحجيم عائلة (2→3) + "أضف الكل" حقيقي → `WorldSwitcher` فتح/إغلاق آمن (`Escape`، بلا طبقة علوية يتيمة) → تبديل ثيم شخصي عبر `localStorage` (لا UI بعد) والتحقق من بقائه بعد إعادة التحميل (توكن `--sb-pt-primary` مطابق للسجل). **21/21 نجحت** من أول تشغيل، صفر أخطاء Console. مراجعة شاملة لكل Outstanding Risks المتراكمة عبر `CHANGELOG.md` الأيام 23-31 — مُصنَّفة (`ATTB`/`RE-EVALUATE`) بالكامل في Task Report هذا اليوم؛ أبرز `RE-EVALUATE`: فجوة `BottomNav` (يوم 28، قرار تجاهل متكرر يستحق حسماً صريحاً)، ومحور التفضيل الشخصي بلا مستهلك واجهة بعد يومين إضافيين (يوم 30). `docs/DOMAIN_MAP.md`/`docs/ARCHITECTURE.md`/`docs/UI_UX_SYSTEM.md` محدَّثة بالكامل لتعكس الأيام 29-31 (كانت متوقفة عند اليوم 28). لا `ADR` جديد، لا لمس `AGENTS.md`/`INVARIANTS.md`/`docs/DECISIONS.md` (تعليمة صريحة). المجموع يبقى 126 (يوم تحقق/توثيق لا كود منتج جديد). راجع `docs/CHANGELOG.md` للتفصيل الكامل |

> **بند مستقبلي صريح (2026-09-05) — بعد إتمام بيان (الأيام 23-32):** لوحة إدارة عالم الأعمال
> (نموذج أولي: عيادات/مراكز طبية) — أول تطبيق حقيقي لعالم الأعمال في Context Engine (`worlds`/
> `user_personas`، الأيام 19-22)، يخدم أيضاً كأساس مستقبلي لنبض (`Nabdh`) عند إطلاقه الكامل
> (`CONSTITUTION §7.3`). مرتبط بـ`docs/DIWAN_VISION.md → Addendum 5` (سؤال الفرع مقابل Tenant
> منفصل، `OPEN_QUESTION` صريح يُحسم عند بدء هذا البند تحديداً). `PLANNED` — لا خطة أيام مفصَّلة بعد.

> **✅ إغلاق Context Engine (الأيام 19-22، 2026-09-04):** أول خلية فعلية (لا نظرية) من `ideas/CONTEXTUAL_WORLDS_RFC.md` — بوابة القرار محسومة (`CONFLICT-006`)، المخطط حي على Supabase (`ADR-018`)، أول مستهلك كود عبر Checkout الحقيقي (`ADR-019`)، سياسة حذف صريحة موثَّقة (`ADR-020`). **النطاق الفعلي المكتمل ضيق جداً عمداً:** عالم واحد فقط (`individuals`)، شخصية واحدة تلقائية لكل عميل عند Checkout، لا واجهة تبديل شخصية، لا تكيّف لأي محرك نواة آخر (بيان/حكيم/برق/تيسير جميعها لا تزال `PROPOSED` بلا كود). راجع `docs/DIWAN_VISION.md → "الحالة الحالية مقابل الرؤية"` للمسافة الكاملة بين هذا وبين الرؤية بعيدة المدى. هذا **لا يُنهي** "Phase 2 Preparation" أعلاه ولا يعني بدء بناء عوالم إضافية — خطوة أولى موثَّقة فقط.

> **✅ إصلاحات حرجة بعد تدقيق شامل (2026-09-05، `CRITICAL-FIXES-FROM-AUDIT-001`):** تدقيق معماري/أمني شامل (`FULL-ARCHITECTURE-SECURITY-AUDIT-001`) نفس اليوم كشف خمس فجوات P0 — عولجت الخمسة بدورة اختبار قبل/بعد حية لكل بند (`docs/DECISIONS.md → ADR-022`): Idempotency على Checkout (قفل Single-Flight)، سباق مخزون TOCTOU حقيقي (خصم بالتزامن المتفائل، مُثبَت بيعاً مضاعفاً فعلياً قبل الإصلاح)، حدود معاملة موثَّقة (تعويض تطبيقي)، وتوحيد عزل المستأجرين على `getOrderWithItems`/`getStatusHistory`. تحقُّق حي مباشر لـRLS (17/17 فحصاً) أغلق آخر بند `UNKNOWN` في التدقيق. لا تغيير DDL — كود تطبيق بحت.
>
> **🚫 BLOCKER صريح — خطورة `HIGH` (صُحِّح 2026-09-05، `GUARDIAN-FINDINGS-REMEDIATION-001` — كان
> مصنَّفاً "قيد معروف" غامضاً، تصحيح تصنيف لا اكتشاف جديد):** قفلان في-الذاكرة من نفس الفئة —
> `src/core/kernel/security/rate-limit.ts` (Rate Limiting على الدخول، `ADR-014`) و`inFlightCheckouts`
> الجديد (Idempotency على Checkout، `ADR-022`) — كلاهما `Map` عملية واحدة، **لا ينجوان من إعادة
> تشغيل الخادم أو تعدد النسخ (Serverless/عدة خوادم)**. مقبولان **حصراً** لمرحلة خادم واحد الحالية
> (dev/`staging.reefam.com`). **يجب إغلاق هذا البند (قفل موزَّع — على الأرجح عبر Supabase نفسها
> بقيد `UNIQUE` مؤقت، أو Redis لاحقاً عند نضج البنية التحتية) قبل أي تفعيل حقيقي لإنتاج متعدد
> الخوادم على `reefam.com` (لا `staging.`)** — لا يوجد بعد بند/يوم مُخطَّط بهذا الاسم في هذا الملف؛
> **أي بند مستقبلي يُضاف تحت عنوان "إطلاق إنتاج حقيقي" أو ما يعادله يجب أن يذكر هذا الـBLOCKER
> صراحة في قيوده قبل الاعتماد.** ببند واحد موحَّد لكلا القفلين، لا بندين منفصلين.

**تفاصيل كل يوم (الترقيم الأصلي 0-14، مع الإزاحات والتمديد حتى اليوم 18):** راجع `SALSABIL_CONSTITUTION.md §23`، القسم الفرعي **"تصحيح تاريخي: من 14 إلى 18 يوماً"**.

---

## 🚨 أولوية عاجلة جديدة — استعداد استقبال 70 تاجراً حقيقياً (سُجِّلت 2026-09-08)

> **مصدر:** إبلاغ مباشر من المؤسس ضمن موجّه `EXTRACT-DESIGN-DNA-AND-APPLY-ACROSS-ALL-SCREENS`
> (المرحلة 2) — "معلومة عاجلة يجب توثيقها الآن قبل أي بناء إضافي". تسجيل نطاق وإلحاح فقط، **لا تنفيذ
> أو قرار معماري متخَذ هنا**.

**الوضع الفعلي المُبلَّغ:**
- 70 تاجراً حقيقياً منتظرون الانضمام.
- ~5000 منتج متوقَّع خلال أسبوع واحد.
- 10 تجار (من الـ70) بمنتجات متطابقة أو شديدة التشابه فيما بينهم.
- 50 تاجراً (من الـ70) لديهم أنظمة كاشير/POS خارجية قائمة فعلياً.

**لماذا هذا عاجل معمارياً (لا رقماً تسويقياً فقط):** النظام الحالي (راجع "Current Phase" أعلاه) بُني
ومُختبَر حياً لحي واحد (`daily-food`) وتاجر تجريبي واحد. القفزة إلى 70 تاجراً حقيقياً، بعضهم بكتالوج
متطابق، تمس مباشرة ثلاثة أسئلة معمارية غير محسومة اليوم:

1. **المصادقة (Authentication):** دخول التاجر اليوم بلا كلمة مرور، بالهاتف فقط (`ADR-012`) — مقبول
   صراحة "لتاجر تجريبي واحد فقط"، ونص القرار نفسه يشترط إغلاقه **"قبل تسجيل تاجر ثانٍ حقيقي"**. 70
   تاجراً يتجاوز هذا الشرط بفارق كبير جداً.
2. **الكتالوج الموحَّد (Unified Catalog):** 10 تجار بمنتجات متطابقة بلا كتالوج مشترك = 10 إدخالات
   منفصلة كاملة لنفس المنتج، وتكرار بصري حقيقي للعميل. راجع `ideas/IDEAS.md → IDEA-004` للتفصيل
   الكامل والسؤال المفتوح.
3. **الاستيراد الجماعي (Bulk Import):** لا آلية استيراد جماعي (CSV/API) موجودة اليوم — إدخال ~5000
   منتج يدوياً غير عملي. 50 تاجراً بأنظمة كاشير خارجية يفتح سؤال تكامل استيراد لم يُدرَس بعد.

**القرار الصريح للمؤسس:** مناقشة جاهزية النظام لاستقبال 70 تاجراً فعلياً (المصادقة، الكتالوج الموحَّد،
الاستيراد الجماعي) **قبل** الانتقال للمرحلة 3 (السلة المتقدمة) من موجّه
`EXTRACT-DESIGN-DNA-AND-APPLY-ACROSS-ALL-SCREENS` الجاري — لا تنفيذ لأي من الأسئلة الثلاثة أعلاه قبل
تلك المناقشة.

---

> **⚠️ ملاحظة إعادة تعريف (2026-09-01، `FOUNDER_DECISION` عبر محادثة UI/UX):** الخطة الأصلية في §23 من الدستور كانت تضع "السلة (Cart)" في اليوم 6. طلب المؤسس مباشرة بناء معمارية الثيمات متعددة العوالم كأولوية اليوم 6 بدلاً منها (راجع `docs/UI_UX_SYSTEM.md §8` و`docs/DECISIONS.md → ADR-007`)، بحجة منطقية: انعدام الهاردكود اللوني في `/src/components` أسهل وأرخص الآن (مكوّنات قليلة، ريف فقط) منه بعد بناء السلة والـ Checkout فوق مكوّنات ملوّنة بالفعل بشكل صلب. **هذا تغيير مرتّب لا حذف** — "السلة" لم تُلغَ، فقط أُزيحت يوماً واحداً (تصبح اليوم 7 فعلياً). راجع `docs/DECISIONS.md → CONFLICT-004` لتوثيق هذا كتصحيح دَين توثيقي.
>
> **⚠️ ملاحظة إزاحة ثانية (2026-09-02، اليوم 8، `CHECKOUT-001`):** موجّه اليوم 8 يفرّق صراحة بين "Checkout" (تحويل سلة → طلب PENDING فقط) و"دورة حياة الطلب الكاملة" (تغيّر الحالة، تعيين مندوب، إلخ — مؤجَّلة لليوم التالي). الجدول القديم كان يضع "دورة حياة الطلب" كاملة في اليوم 8 مباشرة بلا يوم Checkout منفصل — صُحِّح هنا بإدراج يوم Checkout مستقل وإزاحة كل ما بعده يوماً إضافياً، فتجاوزت الخطة الترقيم الأصلي 0-14. **(مُحدَّث 2026-09-02، راجع الملاحظة التالية): السؤال المفتوح هنا — هل تُمدَّد الخطة فعلياً أم يُدمَج يومان للبقاء عند 14 — أصبح محسوماً الآن، وليس لصالح 15 بل 18.**
>
> **✅ ملاحظة تمديد ثالثة ومُحسومة (2026-09-02، `FOUNDER_DECISION` مباشر، DAY-13-CLOSE + ROADMAP-CORRECTION):** بعد إغلاق اليوم 13 (الاختبار الشامل)، قرر المؤسس صراحة أن الخطة تمتد إلى **18 يوماً لا 14 ولا 15** — ثلاثة أيام جديدة كلياً (14-16) لمرحلة الواجهة الأمامية (UI/Frontend) لم تكن في الخطة الأصلية إطلاقاً، ثم التجهيز للإنتاج (17) والإطلاق الحي (18) بدل يوم واحد مدمج كما كان مفترضاً أصلاً. راجع `SALSABIL_CONSTITUTION.md §23` → **"تصحيح تاريخي: من 14 إلى 18 يوماً"** للتفصيل الكامل والسبب.

---

## Phase 2 (بعد نجاح الخلية الأولى) — Evidence: `CONSTITUTION` §35 (Backlog)

- **`PROPOSED` (2026-09-07، `RAPID-VISUAL-REDESIGN-BATCH-SAFE-SCREENS`):** تحويل سجل هوية الأحياء
  البصرية (`src/config/neighborhood-identity-registry.ts`، `ADR-024`) من كود ثابت إلى جدول قاعدة
  بيانات + واجهة إدارة CRUD — يسمح بتغيير لون/هوية حي دون نشر كود جديد. لم يُبنَ بعد، يحتاج تقييم
  حجم الحاجة الفعلية أولاً (اليوم: حي واحد فقط مسجَّل في السجل، `daily-food`).
- توسيع أحياء ريف (مطاعم، عطور، دواجن، أسماك، ألبان...)
- محرك البدائل الذكية (§14)
- نظام الحجز بالسعة (§15)
- نظام الأفيليت والدعوات (§17)
- محرك اكتساب العملاء الخارجي (§18)
- سوق المفوّضين (§28) + سوق برق المفتوح
- محرك المطابقة العادلة (§29.3)
- التخصيص الذكي واللغات (§30)
- محرك مكافحة الإسراف (§31)

---

## Future / Research — بعيدة المدى، لا خطة تنفيذ حالية

- **رؤية الديوان الكاملة (عوالم سياقية + المستويات الثلاثة: ديوان كطبقة تجربة ← فوق أندرويد ← نظام تشغيل مستقل)** — راجع `docs/DIWAN_VISION.md` (جديد، 2026-09-04) للنص الكامل ومقارنة "الحالة الحالية مقابل الرؤية". مرجعي بحت (`VISIONARY`)، لا يُجدوِل أي بند منه هنا تلقائياً.
- أسراب، نبض، نور الدين (عوالم كاملة جديدة) — **الهوية البصرية فقط (ألوان/رموز) صارت `PROPOSED` من اليوم 6، لا يعني هذا بدء بناء واجهاتها الفعلية بعد**
- Rust (Phase 2 تقني) / Elixir-Erlang (Phase 3 تقني) — راجع `docs/ARCHITECTURE.md §6`
- منظومة التطبيقات المصغّرة (Mini-Apps SDK) — سنة 2+ (§27 CONSTITUTION)
- تحوّل سلسبيل لوقف — بعد نضج كامل (§27.1 CONSTITUTION)
- نظام تشغيل سلسبيل والأجهزة (ساعات، خواتم) — Phase 4+ (§33 CONSTITUTION)
- تيسير: مدفوعات شاملة (شحن كروت، QR، تلامسي) — يتطلب شراكات فعلية أولاً (§34 CONSTITUTION)

---

## Deferred — من محادثات لاحقة، خارج نص الدستور v1.2، بانتظار قرار توثيق رسمي

> **مصدر:** محادثة 2026-09-01. المؤسس طلب صراحة تأجيل توثيقها في الدستور نفسه حتى بعد إطلاق المرحلة الأولى، لتفادي تشتيت التركيز عن خطة الـ14 يوماً. راجع `ideas/IDEAS.md` للتفصيل الكامل.

- **رؤية الاستيراد الدولي (Landed Cost Engine):** تعاقد مع شركات استيراد سيارات ومكاتب تجارية في الصين كمزوّدين مسؤولين (Adapter pattern)، سعر شامل نهائي للعميل، تكامل مستقبلي محتمل مع منصة "نافذة" الحكومية المصرية (نظام حقيقي وقائم فعلياً منذ 2021).

---

## قاعدة تحديث هذا الملف

لا يُعتبر أي تاريخ أو ميزة نهائية إلا إذا كانت `ACCEPTED`/`DONE` فعلياً. الحالات المستخدمة هنا: `DONE`, `NEXT`, `PLANNED`, `PROPOSED`, `DEFERRED` فقط.